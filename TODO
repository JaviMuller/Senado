Add optional in documentation for arguments with default values
Add parameter checks
Add unit tests
Define CLI/UI interface
Implement integration tests
Investigate Github actions for branch checking
Investigate whether it makes sense to use any other github features (wiki, projects, etc)

Check ballots with number of votes less than expected